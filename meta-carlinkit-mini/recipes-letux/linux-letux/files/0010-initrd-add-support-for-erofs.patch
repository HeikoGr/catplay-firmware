Date: Sat, 6 Sep 2025 14:43:38 +0200
Subject: [PATCH] initrd: add support for erofs
---
 include/linux/root_dev.h   |   1 +
 include/uapi/linux/major.h |   2 +
 init/Makefile              |   1 +
 init/do_mounts.c           |   6 +
 init/do_mounts.h           |   2 +
 init/do_mounts_initrd.c    |   6 +
 init/do_mounts_rd.c        |  39 +++++-
 init/initramfs.c           |  11 +-
 init/initrd_blk.c          | 237 +++++++++++++++++++++++++++++++++++++
 9 files changed, 301 insertions(+), 4 deletions(-)
 create mode 100644 init/initrd_blk.c

diff --git a/include/linux/root_dev.h b/include/linux/root_dev.h
index 847c9a061..3d9507a32 100644
--- a/include/linux/root_dev.h
+++ b/include/linux/root_dev.h
@@ -11,6 +11,7 @@ enum {
 	Root_CIFS = MKDEV(UNNAMED_MAJOR, 254),
 	Root_Generic = MKDEV(UNNAMED_MAJOR, 253),
 	Root_RAM0 = MKDEV(RAMDISK_MAJOR, 0),
+	Root_INITRD = MKDEV(INITRD_MAJOR, 0),
 };
 
 extern dev_t ROOT_DEV;
diff --git a/include/uapi/linux/major.h b/include/uapi/linux/major.h
index 4e5f2b3a3..5fa4f8b6c 100644
--- a/include/uapi/linux/major.h
+++ b/include/uapi/linux/major.h
@@ -175,4 +175,6 @@
 #define BLOCK_EXT_MAJOR		259
 #define SCSI_OSD_MAJOR		260	/* open-osd's OSD scsi device */
 
+#define INITRD_MAJOR 420
+
 #endif
diff --git a/init/Makefile b/init/Makefile
index 10b652d33..9a47db534 100644
--- a/init/Makefile
+++ b/init/Makefile
@@ -17,6 +17,7 @@ obj-y                          += init_task.o
 
 mounts-y			:= do_mounts.o
 mounts-$(CONFIG_BLK_DEV_RAM)	+= do_mounts_rd.o
+mounts-$(CONFIG_BLK_DEV_RAM)	+= initrd_blk.o
 mounts-$(CONFIG_BLK_DEV_INITRD)	+= do_mounts_initrd.o
 
 #
diff --git a/init/do_mounts.c b/init/do_mounts.c
index 6af29da88..5ce99254f 100644
--- a/init/do_mounts.c
+++ b/init/do_mounts.c
@@ -446,6 +446,8 @@ static dev_t __init parse_root_device(char *root_device_name)
 		return Root_CIFS;
 	if (strcmp(root_device_name, "/dev/ram") == 0)
 		return Root_RAM0;
+	if (strcmp(root_device_name, "/dev/initrd") == 0)
+		return Root_INITRD;
 
 	error = early_lookup_bdev(root_device_name, &dev);
 	if (error) {
@@ -458,6 +460,10 @@ static dev_t __init parse_root_device(char *root_device_name)
 	return dev;
 }
 
+bool __init is_root_initrd(void) {
+	return strcmp(saved_root_name, "/dev/initrd") == 0;
+}
+
 /*
  * Prepare the namespace - decide what/where to mount, load ramdisks, etc.
  */
diff --git a/init/do_mounts.h b/init/do_mounts.h
index 6069ea3eb..aaec94252 100644
--- a/init/do_mounts.h
+++ b/init/do_mounts.h
@@ -50,3 +50,5 @@ static inline void init_flush_fput(void)
 	flush_delayed_fput();
 	task_work_run();
 }
+
+bool __init is_root_initrd(void);
\ No newline at end of file
diff --git a/init/do_mounts_initrd.c b/init/do_mounts_initrd.c
index 22c7f41ff..4077ba16c 100644
--- a/init/do_mounts_initrd.c
+++ b/init/do_mounts_initrd.c
@@ -136,7 +136,13 @@ static void __init handle_initrd(char *root_device_name)
 bool __init initrd_load(char *root_device_name)
 {
 	if (mount_initrd) {
+		if (ROOT_DEV == Root_INITRD) {
+			printk("RAMDISK: performing direct /dev/initrd mount since root=/dev/initrd was used");
+			return false;
+		}
+
 		create_dev("/dev/ram", Root_RAM0);
+
 		/*
 		 * Load the initrd data into /dev/ram0. Execute it as initrd
 		 * unless /dev/ram0 is supposed to be our actual root device,
diff --git a/init/do_mounts_rd.c b/init/do_mounts_rd.c
index ac021ae6e..bdc9c3d66 100644
--- a/init/do_mounts_rd.c
+++ b/init/do_mounts_rd.c
@@ -11,6 +11,7 @@
 
 #include "do_mounts.h"
 #include "../fs/squashfs/squashfs_fs.h"
+#include "../fs/erofs/erofs_fs.h"
 
 #include <linux/decompress/generic.h>
 
@@ -47,6 +48,7 @@ static int __init crd_load(decompress_fn deco);
  *	romfs
  *	cramfs
  *	squashfs
+ *	erofs
  *	gzip
  *	bzip2
  *	lzma
@@ -63,6 +65,7 @@ identify_ramdisk_image(struct file *file, loff_t pos,
 	struct romfs_super_block *romfsb;
 	struct cramfs_super *cramfsb;
 	struct squashfs_super_block *squashfsb;
+	struct erofs_super_block *erofsb;
 	int nblocks = -1;
 	unsigned char *buf;
 	const char *compress_name;
@@ -77,6 +80,7 @@ identify_ramdisk_image(struct file *file, loff_t pos,
 	romfsb = (struct romfs_super_block *) buf;
 	cramfsb = (struct cramfs_super *) buf;
 	squashfsb = (struct squashfs_super_block *) buf;
+	erofsb = (struct erofs_super_block *) buf;
 	memset(buf, 0xe5, size);
 
 	/*
@@ -165,6 +169,19 @@ identify_ramdisk_image(struct file *file, loff_t pos,
 		goto done;
 	}
 
+	/* Try erofs */
+	pos = (start_block * BLOCK_SIZE) + EROFS_SUPER_OFFSET;
+	kernel_read(file, buf, size, &pos);
+
+	if (erofsb->magic == EROFS_SUPER_MAGIC_V1) {
+		printk(KERN_NOTICE
+		       "RAMDISK: erofs filesystem found at block %d\n",
+		       start_block);
+
+		nblocks = DIV_ROUND_UP(i_size_read(file_inode(file)), BLOCK_SIZE);
+		goto done;
+	}
+
 	printk(KERN_NOTICE
 	       "RAMDISK: Couldn't find valid RAM disk image starting at %d.\n",
 	       start_block);
@@ -253,11 +270,29 @@ int __init rd_load_image(char *from)
 			fput(in_file);
 			break;
 		}
-		kernel_read(in_file, buf, BLOCK_SIZE, &in_pos);
-		kernel_write(out_file, buf, BLOCK_SIZE, &out_pos);
+		long r = kernel_read(in_file, buf, BLOCK_SIZE, &in_pos);
+		if (r == 0) {
+			printk(KERN_ERR "RAMDISK: error while reading initrd data at block %d\n", r);
+			goto done;
+		} else if (r < 0) {
+			printk(KERN_ERR "RAMDISK: EOF while reading initrd data at block %d\n", r);
+			goto done;
+		}
+
+		long written = kernel_write(out_file, buf, BLOCK_SIZE, &out_pos);
+		if (written != BLOCK_SIZE) {
+			printk(KERN_ERR
+				"RAMDISK: incomplete write (%ld != %ld)\n",
+				written, BLOCK_SIZE);
+			goto done;
+
+		}
+
 		if (!IS_ENABLED(CONFIG_S390) && !(i % 16)) {
 			pr_cont("%c\b", rotator[rotate & 0x3]);
 			rotate++;
 		}
 	}
+	
+	vfs_fsync(out_file, 0);
 	pr_cont("done.\n");
 
 successful_load:
diff --git a/init/initramfs.c b/init/initramfs.c
index b2f7583bb..add4a32e3 100644
--- a/init/initramfs.c
+++ b/init/initramfs.c
@@ -688,6 +688,11 @@ static void __init populate_initrd_image(char *err)
 	struct file *file;
 	loff_t pos = 0;
 
+	if (is_root_initrd()) {
+		printk(KERN_INFO "RAMDISK: Skipping populate_initrd_image since root=/dev/initrd was requested");
+		return;
+	}
+
 	printk(KERN_INFO "rootfs image is not initramfs (%s); looks like an initrd\n",
 			err);
 	file = filp_open("/initrd.image", O_WRONLY|O_CREAT|O_LARGEFILE, 0700);
@@ -730,20 +735,22 @@ static void __init do_populate_rootfs(void *unused, async_cookie_t cookie)
 done:
 	security_initramfs_populated();
 
+	do_retain_initrd |= is_root_initrd();
+
 	/*
 	 * If the initrd region is overlapped with crashkernel reserved region,
 	 * free only memory that is not part of crashkernel region.
 	 */
 	if (!do_retain_initrd && initrd_start && !kexec_free_initrd()) {
 		free_initrd_mem(initrd_start, initrd_end);
+		initrd_start = 0;
+		initrd_end = 0;
 	} else if (do_retain_initrd && initrd_start) {
 		bin_attr_initrd.size = initrd_end - initrd_start;
 		bin_attr_initrd.private = (void *)initrd_start;
 		if (sysfs_create_bin_file(firmware_kobj, &bin_attr_initrd))
 			pr_err("Failed to create initrd sysfs file");
 	}
-	initrd_start = 0;
-	initrd_end = 0;
 
 	init_flush_fput();
 }
diff --git a/init/initrd_blk.c b/init/initrd_blk.c
new file mode 100644
index 000000000..4f34d7286
--- /dev/null
+++ b/init/initrd_blk.c
@@ -0,0 +1,237 @@
+// SPDX-License-Identifier: GPL-2.0-only
+#include <linux/module.h>
+#include <linux/blkdev.h>
+#include <linux/initrd.h>
+#include <linux/fs.h>
+#include <linux/init.h>
+#include <linux/uaccess.h>
+#include <linux/dax.h>
+#include <linux/pfn.h>
+#include <linux/major.h>
+
+
+struct initrd_device {
+	struct gendisk *disk;
+    struct dax_device *dax;
+    void* start;
+	phys_addr_t size; /* in bytes */
+};
+
+static struct initrd_device *initrd_dev = NULL;
+
+static void initrd_submit_bio(struct bio *bio)
+{
+    struct initrd_device *dev = bio->bi_bdev->bd_disk->private_data;
+    sector_t sector = bio->bi_iter.bi_sector;
+    struct bio_vec bvec;
+    struct bvec_iter iter;
+
+    if (!initrd_start) {
+        pr_err("initrd_blk: initrd memory was freed after initialization. This is a bug!");
+        bio_io_error(bio);
+        return;
+    }
+
+    bio_for_each_segment(bvec, bio, iter) {
+        void *mem;
+        struct page *page = bvec.bv_page;
+        unsigned int len = bvec.bv_len;
+        phys_addr_t offset = (sector << SECTOR_SHIFT);
+
+        if (offset + len > dev->size) {
+            bio_io_error(bio);
+            return;
+        }
+
+        mem = kmap_local_page(bvec.bv_page) + bvec.bv_offset;
+
+        if (!op_is_write(bio->bi_opf)) {
+            pr_debug("initrd_blk: responding to read request at offset %pa", &offset);
+            memcpy(mem, (void *)(initrd_start + offset), len);
+            flush_dcache_page(page);
+        } else {
+            pr_debug("initrd_blk: responding to write request at offset %pa", &offset);
+            flush_dcache_page(page);
+            memcpy((void *)(initrd_start + offset), mem, len);
+        }
+
+        kunmap_local(mem);
+        sector += len >> SECTOR_SHIFT;
+    }
+
+    bio_endio(bio);
+}
+
+static const struct block_device_operations initrd_fops = {
+	.owner = THIS_MODULE,
+	.submit_bio = initrd_submit_bio,
+};
+
+#ifdef CONFIG_DAX
+static long initrd_dax_direct_access(struct dax_device *dax_dev,
+				     pgoff_t pgoff, long nr_pages,
+                     enum dax_access_mode mode,
+				     void **kaddr, unsigned long *pfn)
+{
+	unsigned long offset = pgoff << PAGE_SHIFT;
+	if (offset + (nr_pages << PAGE_SHIFT) > initrd_dev->size)
+		return -ERANGE;
+
+    if (!initrd_start) {
+        pr_err("initrd_blk: initrd memory was freed after initialization. This is a bug!");
+        return -ENODEV;
+    }
+
+    pr_debug("initrd_blk: responding to DAX request at offset %llu", offset);
+
+	*kaddr = (void *)(initrd_start + offset);
+    *pfn = PHYS_PFN(__pa(initrd_start + offset));
+	return nr_pages;
+}
+
+static int initrd_dax_zero_page_range(struct dax_device *dax_dev,
+                                      pgoff_t pgoff, size_t size)
+{
+    unsigned long offset = pgoff << PAGE_SHIFT;
+
+    if (!initrd_start) {
+        pr_err("initrd_blk: initrd memory was freed after initialization. This is a bug!");
+        return -ENODEV;
+    }
+
+    if (offset + size > initrd_dev->size)
+        return -ERANGE;
+
+    memset((void *)(initrd_start + offset), 0, size);
+    return 0;
+}
+
+static const struct dax_operations initrd_dax_ops = {
+	.direct_access = initrd_dax_direct_access,
+    .zero_page_range = initrd_dax_zero_page_range,
+};
+#endif
+
+static int __init initrd_blk_init(void)
+{
+	struct gendisk *disk;
+	struct queue_limits lim = {
+		.physical_block_size = PAGE_SIZE,
+		.features            = BLK_FEAT_SYNCHRONOUS |
+		                       BLK_FEAT_NOWAIT |
+		                       BLK_FEAT_DAX,
+	};
+	phys_addr_t size;
+	int err;
+
+	if (!initrd_start || !initrd_end) {
+		pr_err("initrd_blk: no initrd found\n");
+		return -ENODEV;
+	}
+
+	if (initrd_start & (PAGE_SIZE - 1)) {
+		pr_err("initrd_blk: initrd provided at address not aligned to page size: %#lx\n",
+		       (unsigned long)initrd_start);
+		return -EINVAL;
+	}
+
+	size = initrd_end - initrd_start;
+
+	if (size & (PAGE_SIZE - 1)) {
+		pr_err("initrd_blk: initrd size not aligned to page size: %pa (0x%llx)\n",
+		       &size, (unsigned long long)size);
+		return -EINVAL;
+	}
+
+	initrd_dev = kzalloc(sizeof(*initrd_dev), GFP_KERNEL);
+	if (!initrd_dev)
+		return -ENOMEM;
+
+	initrd_dev->size  = size;
+	initrd_dev->start = (void *)initrd_start;
+
+	disk = blk_alloc_disk(&lim, NUMA_NO_NODE);
+	if (IS_ERR(disk)) {
+		err = PTR_ERR(disk);
+		goto out_free_dev;
+	}
+
+	initrd_dev->disk = disk;
+	disk->major        = INITRD_MAJOR;
+	disk->first_minor  = 0;
+	disk->minors       = 1;
+	disk->fops         = &initrd_fops;
+	disk->private_data = initrd_dev;
+	strscpy(disk->disk_name, "initrd", DISK_NAME_LEN);
+
+#ifdef CONFIG_DAX
+	initrd_dev->dax = alloc_dax(initrd_dev, &initrd_dax_ops);
+	if (IS_ERR(initrd_dev->dax)) {
+		err = PTR_ERR(initrd_dev->dax);
+		goto out_put_disk;
+	}
+
+	err = dax_add_host(initrd_dev->dax, disk);
+	if (err) {
+		put_dax(initrd_dev->dax);
+		goto out_put_disk;
+	}
+#endif
+
+	set_capacity(disk, size >> SECTOR_SHIFT);
+
+	err = add_disk(disk);
+	if (err)
+		goto out_dax_cleanup;
+
+#ifdef CONFIG_DAX
+	pr_info("initrd_blk: registered /dev/initrd size=%llu KB with DAX\n",
+		(unsigned long long)(size >> 10));
+#else
+	pr_info("initrd_blk: registered /dev/initrd size=%llu KB\n",
+		(unsigned long long)(size >> 10));
+#endif
+	return 0;
+
+out_dax_cleanup:
+#ifdef CONFIG_DAX
+	dax_remove_host(disk);
+	put_dax(initrd_dev->dax);
+#endif
+out_put_disk:
+	put_disk(disk);
+out_free_dev:
+	kfree(initrd_dev);
+	initrd_dev = NULL;
+	return err;
+}
+
+static void __exit initrd_blk_exit(void)
+{
+	if (!initrd_dev)
+		return;
+
+	if (initrd_dev->disk) {
+		del_gendisk(initrd_dev->disk);
+#ifdef CONFIG_DAX
+		if (initrd_dev->dax) {
+			dax_remove_host(initrd_dev->disk);
+			put_dax(initrd_dev->dax);
+		}
+#endif
+		put_disk(initrd_dev->disk);
+	}
+
+	kfree(initrd_dev);
+	initrd_dev = NULL;
+
+	pr_info("initrd_blk: unloaded\n");
+}
+
+module_init(initrd_blk_init);
+module_exit(initrd_blk_exit);
+
+MODULE_LICENSE("GPL");
+MODULE_DESCRIPTION("Initrd-backed block device");
+MODULE_ALIAS_BLOCKDEV_MAJOR(INITRD_MAJOR);
+MODULE_ALIAS("initrd");
-- 
2.51.0
